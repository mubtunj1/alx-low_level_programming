#include "main.h"

/**
 * _abs - the absolute value of an integer
 * @t: the integer to be compute
 * Return: the absolute integer
 */
int _abs(int t)
{
	if (t >= 0)
	{
		return (t);
	}
	else
	{
		return (-t);
	}
}
